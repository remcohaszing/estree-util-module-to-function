/** @param {import('estree-util-module-to-function').Import} customImport */
export default async (customImport) => {
  'use strict'
  const [...more] = counts
  return {
    __proto__: null,
    more
  }
}
